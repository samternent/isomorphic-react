var React = require('react');

module.exports = React.createClass({
  displayName: 'record',
  
  render: function () {
    return React.DOM.div(null,
      'this is the account page layout'
      );
  }
});
